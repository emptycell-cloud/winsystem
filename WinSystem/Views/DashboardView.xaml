<UserControl x:Class="WinSystem.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:v="clr-namespace:WinSystem.Views">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="26,22,26,30">

            <!-- 欢迎横幅 -->
            <Border CornerRadius="16" Padding="26,22" Background="{StaticResource PrimaryGradientBrush}">
                <Grid>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="{Binding Greeting}" FontSize="26" FontWeight="SemiBold"
                                   Foreground="White"/>
                        <TextBlock Text="{Binding GreetingSub}" FontSize="14" Foreground="#EAF0FF"
                                   Margin="0,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Clock, Mode=OneWay}" Foreground="White" FontSize="15"
                               HorizontalAlignment="Right" VerticalAlignment="Top" FontFamily="Consolas" Opacity="0.9"/>
                </Grid>
            </Border>

            <!-- 近 3 天日程（半宽） -->
            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,20,0">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="近 3 天日程" Style="{StaticResource SectionTitle}"/>
                            <TextBlock Text="点击日程可进入日程管理 ›" FontSize="12"
                                       HorizontalAlignment="Right" VerticalAlignment="Center"
                                       Foreground="{StaticResource TextMutedBrush}"/>
                        </Grid>
                        <TextBlock Text="{Binding ScheduleSummary}" Foreground="{StaticResource TextMutedBrush}"
                                   FontSize="12" Margin="0,4,0,0"/>
                        <ItemsControl ItemsSource="{Binding RecentSchedules}" Margin="0,10,0,0">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F6F8FC" CornerRadius="8" Padding="12,10" Margin="0,0,0,8"
                                            Cursor="Hand">
                                        <Border.InputBindings>
                                            <MouseBinding Gesture="LeftClick" CommandParameter="{Binding}"
                                                          Command="{Binding DataContext.OpenScheduleCommand,
                                                                          RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                                        </Border.InputBindings>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontSize="14" FontWeight="SemiBold"
                                                           Foreground="{StaticResource TextPrimaryBrush}"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <TextBlock Text="{Binding DateText}" FontSize="12"
                                                               Foreground="{StaticResource TextMutedBrush}"/>
                                                    <TextBlock Text=" · " FontSize="12"
                                                               Foreground="{StaticResource TextMutedBrush}"/>
                                                    <TextBlock Text="{Binding TimeText}" FontSize="12"
                                                               Foreground="{StaticResource TextMutedBrush}"/>
                                                </StackPanel>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding StatusText}" FontSize="12"
                                                       Foreground="{StaticResource TextSecondaryBrush}"
                                                       VerticalAlignment="Center"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </Grid>

            </StackPanel>
    </ScrollViewer>
</UserControl>